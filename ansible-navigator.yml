---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - ./inventory/hosts
  execution-environment:
    pull:
      policy: always
    image: quay.io/mschreie/bcl-ov:10
  playbook-artifact:
    enable: false
  mode: stdout